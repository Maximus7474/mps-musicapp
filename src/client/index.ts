// import { triggerServerCallback } from "./utils/callbacks";

// RegisterNuiCallback('example-callback', async (_: null, cb: (data: unknown) => void) => {
//   const data = await triggerServerCallback('example:servercallback');
//   cb(data);
// });
